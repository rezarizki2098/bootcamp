<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Product</title>
</head>
<body>
    <form action="/product/<%= products._id %>?_method=PUT" method="POST">
        <label for="name">Product Name</label>
        <input type="text" name="name" placeholder="Product Name" value="<%= products.name %>">
        <label for="price">Price (unit)</label>
        <input type="number" id="price" name="price" placeholder="Product Price" value="<%= products.price %>">
        <label for="category">Select Category</label>
        <select name="category" id="category">
            <% for (let category of categories) {%>
                <option value="<%= category %>" <%= products.category === category ? 'selected' : '' %>><%= category %></option>
            <% } %>
            <!-- ? <%= products.category === 'dairy' ? 'selected' : '' %> kita bisa menggunakan kode ini jika data dari select tidak banyak tapi kalau terlalu banyak code ini tidak dianjurkan -->
        </select>
        <button>Submit</button>
    </form>
    <a href="/product/<%= products._id %>">Cancel</a>
</body>
</html>